<ui:DialogWindow
    x:Class="C4rl1t0sRefactor.RefactorMenuWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
    xmlns:shell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
    Title="C4rl1t0sRefactor"
    Width="420"
    SizeToContent="Height"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner"
    Background="{DynamicResource {x:Static shell:VsBrushes.WindowKey}}"
    Foreground="{DynamicResource {x:Static shell:VsBrushes.WindowTextKey}}">

    <Border Padding="12">
        <StackPanel>
            <TextBlock Text="C4rl1t0sRefactor"
                       FontWeight="SemiBold"
                       FontSize="14"
                       Margin="0,0,0,8"
                       Foreground="{DynamicResource {x:Static shell:VsBrushes.WindowTextKey}}" />

            <ListBox x:Name="RefactorList"
                     BorderThickness="0"
                     Background="Transparent"
                     MaxHeight="440"
                     Foreground="{DynamicResource {x:Static shell:VsBrushes.WindowTextKey}}"
                     MouseDoubleClick="RefactorList_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="2,3">
                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Setter Property="Opacity" Value="1" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsAvailable}" Value="False">
                                            <Setter Property="Opacity" Value="0.4" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="26" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Number}" Opacity="0.6" />
                            <TextBlock Grid.Column="1" Text="{Binding Label}" TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock Margin="2,10,0,0"
                       FontSize="11"
                       Opacity="0.6"
                       Text="Flechas: elegir  |  Enter: ejecutar  |  Esc: cancelar  |  1-9: acceso rapido" />
        </StackPanel>
    </Border>
</ui:DialogWindow>
